# @package _global_

defaults:
  - ord
  - _self_

# AWS-IAD specific config args
server: "aws-iad-cs-002-login-01.nvidia.com" # login server

# This image is last updated by Jiahui Huang (jiahuih@nvidia.com) to include inter-node RDMA userlib.
bazel:
  dev_image_path: "/lustre/fsw/portfolios/av/users/jiahuih/containers/nre-ord-dev-v26.4.90_c6280fa2_dev.sqsh"

team: "av_alpamayo_cosmos"
exec_path:
  remote: "/lustre/fsw/portfolios/av/users/${user}/nre_experiments" # default path on IAD

partition:
  - pool0_av
