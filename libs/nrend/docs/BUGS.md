## Missing instances when rendering 3DGUT DNSG with NRend

## Crash when rendering in OV with Multi-GPU

## Artifacts when rendering in OV with OpenCVPinhole sensor

## Artifacts when rendering in OV with aspect ratio different from the original sensor one
