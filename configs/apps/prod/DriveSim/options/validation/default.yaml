# @package _global_

dataset:
  n_val_image_subsample: 4  # Reduction factor of rendered frame resolution w.r.t. input resolution
  val_camera_frame_step: 1  # Validation on every camera frame
  val_lidar_frame_step: 1
  val_lidar: false

system:
  test:
    save_results: true
    save_inputs: true
    save_videos: true
    video_fps: 10
