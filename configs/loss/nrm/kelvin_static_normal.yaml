# @package _global_
# Kelvin static loss configuration with normal supervision
# Inherits from kelvin_static and adds normal loss

defaults:
  - kelvin_static

# Adding normal loss on top of base kelvin_static losses
normal:
  fn: normal_cosine
  reduce:
    name: mean
  lambda_: 0.1
  allow_missing_supervision: false

